"""Версия 0.1 — что изменилось за неделю 16–23.09.2026.

Как и 0.0 (см. 0002), создаётся черновиком и публикуется в админке действием
«Опубликовать и оповестить пользователей»: миграция не проходит через
Release.save() и никого не оповещает. Существующую 0.1 не трогает.
"""
import datetime

from django.db import migrations

VERSION = '0.1'

TITLE = 'Новая страница товаров, проверка импорта прайсов, вход через Bitrix24'

SUMMARY = (
    'Изменения за 16–23 сентября. Главная страница теперь — список товаров '
    'из PIM с поставщиками под каждым товаром и экспортом в Excel. Импорт '
    'прайса проверяется перед применением и может ждать подтверждения. '
    'Приоритеты поставщиков правятся прямо в таблице. Вход через Bitrix24, '
    'кнопка «Разработчикам». Наценка от пустой цены больше не ставит цену.'
)

ARTICLE_HTML = """
<p>Что изменилось за неделю с 16 по 23 сентября 2026 года. Полное описание сайта —
в <a href="/releases/0.0/">версии 0.0</a>.</p>

<h2>Главная страница — теперь страница товаров</h2>
<p>Старая главная со списком строк прайса удалена. Вместо неё по логотипу
<strong>PriceManager</strong> открывается страница <strong>Товары</strong>:</p>
<ul>
  <li><strong>Строка — это товар из PIM</strong>, а не строка прайса. Предложения поставщиков
  раскрываются под товаром кликом по строке: цены, остаток, статус наличия, срок поставки,
  кнопки «Добавить в заявку» и «Карточка».</li>
  <li><strong>Поиск</strong> — по названию, артикулу, ключевым словам и по названию у поставщика;
  лучшие совпадения выше.</li>
  <li><strong>Фильтры</strong> слева закреплены при прокрутке: наличие, себестоимость от/до,
  дерево категорий, бренды, поставщики. Выбор категории теперь включает все её подкатегории.</li>
  <li><strong>Выдача сгруппирована по категориям</strong> с заголовками. Сортировка по столбцу
  группировку выключает, «Сбросить сортировку» возвращает её.</li>
  <li><strong>Выбор столбцов</strong> перенесён со старой главной и запоминается для вас.</li>
  <li><strong>Фото</strong> из PIM теперь видны всем (раньше — только тем, кто был залогинен в PIM),
  при наведении на миниатюру фото увеличивается.</li>
  <li>Исправлено: поиск больше не сбрасывает отмеченные фильтры.</li>
</ul>
<p>Старые закладки на <code>/mainproduct/</code> ведут на новую страницу, но фильтры из таких
ссылок не переносятся — настройте их заново.</p>

<h3>Категории и бренды — из PIM</h3>
<ul>
  <li>Старый справочник категорий и производителей удалён. Категории и бренды теперь берутся из PIM;
  в заявках вместо производителя показывается бренд.</li>
  <li>У строк поставщиков больше нет своих описания, категорий, производителя и габаритов — это
  данные товара в PIM.</li>
  <li>В настройках загрузки прайсов пропали варианты столбцов «Производитель» и «Категория».</li>
  <li>В правилах наценки категории выбираются из дерева PIM.</li>
</ul>

<h3>Счётчики под таблицей</h3>
<p>«Без данных PIM» и «Строк поставщиков без товара» показывают, что пока не попадает на страницу
полностью. Строки поставщиков связываются с товарами PIM по артикулу каждую ночь.</p>

<h3>Артикулы без учёта регистра</h3>
<p>«ABC123» и «abc123» теперь один и тот же товар. Такие дубли объединены.</p>

<h2>Экспорт товаров в Excel</h2>
<p>Новая кнопка <strong>Экспорт</strong> на странице товаров выгружает всю текущую выдачу — с поиском,
фильтрами, сортировкой и выбранными столбцами. Файл готовится в фоне, ссылка приходит в «Оповещения».</p>
<ul>
  <li>Лист <strong>«Товары»</strong>: товар и основные цены и остаток — у поставщика с наивысшим
  приоритетом, у которого значение есть.</li>
  <li>По отдельному листу на каждого поставщика — все его значения.</li>
</ul>

<h2>Поставщики</h2>
<ul>
  <li><strong>Статусы обновления</strong> — значки «Цены» и «Остатки» у названия поставщика:
  в срок, просрочено или не отслеживается. Дата и интервал — в подсказке.</li>
  <li><strong>Интервал обновления</strong> задаётся числом в днях или неделях; пусто — не
  отслеживать. Прежние значения переведены в дни автоматически.</li>
  <li><strong>Приоритеты цен и остатков</strong> правятся прямо в ячейке таблицы, без перезагрузки.
  Нумерация всегда сплошная, 1…N без повторов: если поставить поставщику занятый номер, остальные
  сдвинутся сами (сдвинутые ячейки подсвечиваются); если убрать номер или удалить поставщика,
  нумерация сомкнётся. Существующие приоритеты приведены к такой нумерации с сохранением порядка.</li>
  <li>Исправлено: цифры покрытия ценами стояли не под своими заголовками.</li>
</ul>

<h2>Импорт прайсов</h2>

<h3>Проверка перед применением</h3>
<p>Новый файл сравнивается с предыдущими импортами той же настройки. Если строк с ценой или с остатком
заметно меньше обычного, импорт <strong>не применяется</strong>, а получает статус
<strong>«Ждёт подтверждения»</strong>. Ссылка на окно проверки придёт в оповещении; статус виден
и в новой колонке <strong>«Последний импорт»</strong> на вкладке «Настройки загрузки файлов».</p>
<ul>
  <li>В окне — причина, сколько строк будет записано, сколько новых и сколько пропадёт из файла
  (у них очистятся цены и остаток), и подробный разбор файла.</li>
  <li><strong>Применить всё равно</strong> применяет ровно тот файл и то сопоставление, что были
  проверены. Если после проверки поменяли файл или настройку, нужно запустить импорт заново.</li>
  <li><strong>Отменить импорт</strong> — данные не меняются.</li>
  <li>Первые три загрузки новой настройки спрашивают подтверждение всегда — пока копится история.</li>
</ul>

<h3>Понятные отказы</h3>
<p>Если импортировать нечего (нет файла, пустой лист, нет столбца артикула или названия, ни одна строка
не совпала с товарами), импорт останавливается, <strong>данные не меняются</strong>, а в оповещении
пишется причина. Раньше такой файл мог стереть цены и остатки всех товаров поставщика.</p>

<h3>Предупреждения о повторах</h3>
<p>Импорт не останавливается, но предупреждает («Внимание: …» в оповещении):</p>
<ul>
  <li>о повторах строк — берётся первая;</li>
  <li>об артикулах, которые встречаются с разными названиями, — они загружаются как разные товары.
  Обычно это варианты одного товара, но иногда — ошибка в прайсе.</li>
</ul>

<h3>Новый флажок «Артикул уникален — сопоставлять только по артикулу»</h3>
<p>Заменил прежнюю настройку, которая действовала только при создании новых товаров (её значение
перенесено автоматически). С флажком товар определяется одним артикулом: если в файле изменилось
название, товар переименовывается, а не создаётся заново — история и привязка сохраняются. Без флажка,
как и раньше, товар — это пара «артикул + название».</p>

<h3>Итог импорта</h3>
<p>В оповещении теперь видно, сколько строк с ценой и с остатком, сколько новых, переименованных и
отсутствующих в файле.</p>

<h3>Мелкие исправления</h3>
<ul>
  <li>Повреждённый или защищённый паролем файл больше не создаёт пачку настроек «(1)», «(2)»… —
  сразу сообщение об ошибке.</li>
  <li>После смены листа в настройке показываются столбцы нового листа, а не старого.</li>
  <li>Последний загруженный файл настройки и файл, ждущий подтверждения, не удаляются автоочисткой.</li>
</ul>

<h2>Наценки: пустая цена — не ноль</h2>
<p>Если у товара нет цены-источника (пусто или ноль), рассчитанная цена теперь <strong>очищается</strong>.
Раньше в этом случае товар получал цену, равную одной надбавке (или ноль), а при заданном диапазоне цен
сохранял старую цену навсегда. Очистка идёт по цепочке (например, себестоимость → базовая цена → цена ИМ)
и записывается в историю изменений карточки.</p>
<p>Поэтому после обновления у части товаров цены могут стать пустыми — это те, у кого поставщик цену
не дал. Фиксированная цена и наценки от настоящей цены не затронуты.</p>

<h2>Вход через Bitrix24</h2>
<ul>
  <li>На странице входа — кнопка <strong>Войти через Bitrix24</strong>. Вход по паролю остаётся
  запасным.</li>
  <li>Аккаунт Bitrix24 привязывается к учётной записи один раз. Если сайт попросит войти по паролю —
  войдите, и он сразу предложит привязать Bitrix24.</li>
</ul>

<h2>Кнопка «Разработчикам»</h2>
<p>В меню появилась кнопка <strong>Разработчикам</strong>: опишите ошибку или идею, и сообщение станет
задачей в Bitrix24. Адрес страницы, с которой вы пишете, прикладывается сам.</p>

<h2>Оповещения и «Что нового»</h2>
<ul>
  <li>В панели оповещений — кнопка <strong>Удалить все</strong>.</li>
  <li>Раздел <strong>Еще → Что нового</strong>: обзоры версий. О каждой новой версии придёт оповещение
  со ссылкой на статью.</li>
</ul>

<h2>На что обратить внимание</h2>
<ul>
  <li>Обновите закладки: главная — страница товаров.</li>
  <li>Если импорт «Ждёт подтверждения», решите, применять его или нет: пока вы не решили, данные
  поставщика не обновляются.</li>
  <li>Пустые цены после пересчёта наценок — не ошибка, а отсутствие цены у поставщика.</li>
  <li>Проверьте приоритеты поставщиков: от них зависят цены и остатки на листе «Товары» при экспорте.</li>
</ul>
"""


def create_release(apps, schema_editor):
    from releases.rendering import clean_html, html_to_markdown

    Release = apps.get_model('releases', 'Release')
    if Release.objects.filter(version=VERSION).exists():
        return
    # Historical models have no custom save(): clean the HTML and build the
    # Markdown copy here, as Release.save() would.
    article_html = clean_html(ARTICLE_HTML)
    Release.objects.create(
        version=VERSION,
        title=TITLE,
        summary=SUMMARY,
        article_html=article_html,
        article_markdown=html_to_markdown(article_html),
        released_at=datetime.date(2026, 9, 23),
        is_published=False,
    )


def delete_release(apps, schema_editor):
    Release = apps.get_model('releases', 'Release')
    Release.objects.filter(version=VERSION, notified_at__isnull=True).delete()


class Migration(migrations.Migration):

    dependencies = [
        ('releases', '0002_release_0_0_user_guide'),
    ]

    operations = [
        migrations.RunPython(create_release, delete_release),
    ]
